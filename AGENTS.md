# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

## Project Pre-Edit Workflow Exception

For this project, apply requested file changes immediately without creating temporary planned files, opening a pre-change VS Code diff, or waiting for IDE confirmation. Open a diff only when the user explicitly asks for one, then verify the applied changes normally.
